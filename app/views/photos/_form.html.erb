<%= form_for(@photo) do |f| %>
  <% if @photo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@photo.errors.count, "error") %> prohibited this photo from being saved:</h2>

      <ul>
      <% @photo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="field form-group">
    <%= f.label :name %><br/>
    <%= f.text_field :name %>
  </div>
  <div class="field form-group">
    <%= f.label :category %><br/>
    <%= f.select "category", options_for_select(PhotosHelper::CATEGORIES_ENUM) %>
  </div>
  <div class="field form-group">
    <% if !@photo.image_file_name.nil? %> 
      <%= image_tag @photo.image.url %><br/>
    <% end %>
    <%= f.file_field :image %>
  <div class="actions">
    <%= f.submit class: "btn btn-primary" %>
  </div>
<% end %>
